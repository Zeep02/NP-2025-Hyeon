# 브로드캐스트 연습문제 프로토콜

| 구분 | 내용 |
| --- | --- |
| 요청 | `broadcast_client.py`를 여러 호스트에서 실행, `<broadcast>:10000`으로 메시지 송신. |
| 응답 (정상) | 브로드캐스트 서버들이 수신한 메시지를 콘솔에 출력; 별도 응답 메시지는 없음. |
| 응답 (예외) | 응답 자체가 없으므로 발생하지 않음. 필요 시 응답용 별도 포트 설계 필요. |
| 오류 조건 | `SO_BROADCAST` 미설정, 네트워크 장비가 브로드캐스트 필터링, 포트 미칭 등. |

